Add invitation link validation, BBE logo to emails, busy states

- accept-invitation page now validates the token on load via new public
  GET /api/v1/invitations/validate and shows explicit invalid/expired/
  cancelled/used screens (BR-02 L296/L390: old links are visibly dead
  after resend or cancel instead of showing a working-looking form)
- invitation emails and the accept page use the real BBE logo image
  instead of a letter placeholder
- resend/cancel buttons show "Dang gui..."/"Dang huy..." busy state and
  disable while running (chapter-manager + admin pages)
- verified business rules against ba-document.md: resend rotates the
  token hash so the previous link stops validating, cancelled/expired
  tokens are rejected at accept, invitation is one-time use

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
